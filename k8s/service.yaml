apiVersion: v1
kind: Service
metadata:
  name: rbac-demo-app-svc
  namespace: production
spec:
  type: NodePort
  selector:
    app: rbac-demo-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30090
